# Модуль 5. Race conditions и мьютексы — чек-лист готовности

## Поведенческие маркеры

### До защиты (часть 1)

- [ ] Запустил сервер с `go run -race .`
- [ ] Запустил нагрузочный тест (100+ параллельных POST)
- [ ] Увидел в логе **WARNING: DATA RACE** хотя бы один раз
- [ ] Запомнил/записал, сколько задач реально создалось (может быть меньше 100)

### После защиты (часть 2)

- [ ] В `TaskList` добавлено поле `mu sync.Mutex` (с маленькой буквы, без JSON-тега)
- [ ] Импортирован пакет `sync`
- [ ] Метод `All` использует `Lock + defer Unlock`
- [ ] Метод `All` возвращает **копию** слайса через `make + copy`
- [ ] Метод `GetByID` использует `Lock + defer Unlock`
- [ ] Метод `Add` использует `Lock + defer Unlock`
- [ ] Метод `Update` использует `Lock + defer Unlock`
- [ ] Метод `Remove` использует `Lock + defer Unlock`
- [ ] **`findIndex` либо удалён, либо переименован в `findIndexUnlocked` с комментарием**
- [ ] Нет вложенных `Lock` в одной горутине (нет deadlock)
- [ ] Запустил `go run -race .` и повторил нагрузочный тест
- [ ] WARNING DATA RACE **больше не появляется**
- [ ] Количество задач после теста — **ровно** ожидаемое
- [ ] Все ID **уникальны**
- [ ] Все одиночные эндпоинты (curl с разными методами) продолжают работать
- [ ] `go test -race ./...` выполняется чисто (даже если тестов нет)
- [ ] Код закоммичен

## Концептуальные маркеры

- [ ] Могу объяснить, что такое **race condition** в одном предложении
- [ ] Знаю разницу между **race condition** и **data race**
- [ ] Понимаю, что Go обрабатывает каждый HTTP-запрос **в отдельной горутине**
- [ ] Могу объяснить, **почему** `append` без защиты — это data race
- [ ] Могу объяснить, **почему** `NextID++` без защиты тоже опасен
- [ ] Знаю, что такое **мьютекс** и зачем он нужен
- [ ] Понимаю, **почему** `Unlock` принято делать через `defer`
- [ ] Могу объяснить, **почему** все методы `TaskList` должны быть защищены, а не только пишущие
- [ ] **Понимаю, почему `All` должен возвращать копию, а не оригинал**
- [ ] Знаю, что `sync.Mutex` в Go **не реентерабельный**
- [ ] Могу объяснить, что такое **deadlock**
- [ ] Знаю про существование `sync.RWMutex` и в каких случаях он лучше
- [ ] Знаю, что такое **race detector** и команду `go run -race`

## Маркеры экспериментов

- [ ] **Воспроизвёл race condition своими глазами** (это самое важное)
- [ ] Прочитал WARNING DATA RACE из лога — понял структуру сообщения
- [ ] Попробовал забыть `defer Unlock`, увидел deadlock
- [ ] Попробовал вложенный `Lock`, увидел deadlock от нереентерабельности
- [ ] Убрал `copy` из `All` и снова воспроизвёл race на возврате слайса
- [ ] (Опционально) Попробовал `RWMutex` для read-heavy сценария
- [ ] (Бонус) Написал параллельный тест на `Add` с `sync.WaitGroup`

## Самопроверка

Не подсматривая, ответь:

1. Какие два метода у `sync.Mutex`?
2. Почему `Unlock` через `defer`?
3. Что произойдёт, если я забуду залочить один из методов?
4. Что произойдёт, если я возьму `tl.Tasks` напрямую из `All` и буду читать его снаружи?
5. Что произойдёт, если я попытаюсь захватить мьютекс дважды из одной горутины?
6. Что лучше для read-heavy: `Mutex` или `RWMutex`?

## Подготовка к модулю 6

В модуле 6 ты подключишь **сохранение в файл** из Проекта 3. Сейчас твоё состояние живёт **только в памяти** — после перезапуска сервера всё пропадает. После модуля 6 — будет переживать перезапуск.

Главные вопросы модуля 6:

- **Когда** сохранять — после каждого изменения, периодически, или по событию?
- **Где** в коде вызывать `Save` — в обработчике (после действия) или внутри методов `TaskList`?
- **Как** интегрировать `Save` с мьютексами — он же тоже работает с состоянием
- **Что** делать при ошибке записи — упасть, повторить, проигнорировать?

В модуле 6 ты возьмёшь готовые `Save` и `Load` из Проекта 3 (или напишешь снова) и подключишь их к серверу. Это будет приятный завершающий модуль — никаких новых концепций, только чистая интеграция.

Поздравляю — самый сложный модуль проекта позади!
