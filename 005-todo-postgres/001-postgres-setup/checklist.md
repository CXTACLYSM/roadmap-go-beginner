# Модуль 1. Установка и базовый SQL — чек-лист готовности

## Поведенческие маркеры

- [ ] Установлен Docker (`docker --version` работает)
- [ ] Установлен `psql` (`psql --version` работает)
- [ ] Запущен контейнер `todo-pg` с PostgreSQL 16
- [ ] Контейнер использует именованный volume `todo-pg-data`
- [ ] `docker ps` показывает контейнер в статусе `Up`
- [ ] Подключение через `psql -h localhost -U postgres -d postgres` работает
- [ ] Создана таблица `tasks` со всеми тремя колонками и правильными типами
- [ ] `\dt` показывает таблицу `tasks`
- [ ] `\d tasks` показывает структуру с `SERIAL`, `NOT NULL` и `DEFAULT`
- [ ] Сделано **минимум 3 INSERT** с разными задачами
- [ ] Выполнены **все** SELECT-запросы из требований (полный, по id, по done)
- [ ] Сделан **минимум 1 UPDATE** одной задачи
- [ ] Сделан **минимум 1 DELETE** одной задачи
- [ ] Финальный SELECT показывает корректное состояние
- [ ] Контейнер пережил `docker stop && docker start` без потери данных

## Концептуальные маркеры

- [ ] Могу объяснить, что такое **реляционная БД** и чем она отличается от файла
- [ ] Знаю, **почему** PostgreSQL — выбор по умолчанию для бэкенда
- [ ] Понимаю, **зачем** нужен флаг `-v` при `docker run`
- [ ] Знаю, что такое **именованный volume** и чем он отличается от bind mount
- [ ] Могу объяснить, что такое `SERIAL` и зачем он
- [ ] Знаю, что такое `PRIMARY KEY` и какое у него поведение по умолчанию
- [ ] Понимаю, **зачем** ставить `NOT NULL` по умолчанию
- [ ] Могу написать `INSERT`, `SELECT`, `UPDATE`, `DELETE` без подсказок
- [ ] Знаю, что **`UPDATE`/`DELETE` без `WHERE` затронут все строки**, и понимаю, чем это опасно
- [ ] Знаю, что в SQL **строки в одинарных кавычках**, а двойные — для имён колонок
- [ ] Могу объяснить, что такое `RETURNING *` и зачем оно

## Маркеры экспериментов

- [ ] Попробовал INSERT с указанным id, увидел особенность работы sequences
- [ ] Сделал UPDATE без WHERE на тестовых данных, осознал риск
- [ ] Попробовал INSERT с NULL в title, увидел работу NOT NULL
- [ ] Использовал COUNT(\*), увидел простоту агрегаций
- [ ] Использовал ORDER BY с одним и несколькими полями
- [ ] Попробовал LIMIT и OFFSET (основа пагинации)
- [ ] Попробовал RETURNING — без `*` и с `*`
- [ ] (Опционально) Удалил и пересоздал контейнер, убедился, что данные на месте благодаря volume

## Самопроверка

Не подсматривая, ответь:

1. Какие три колонки у нашей таблицы `tasks`? Каких типов?
2. Какая команда `psql` показывает структуру таблицы?
3. Что такое `SERIAL`?
4. Как написать INSERT, который вернёт созданную строку целиком?
5. Что произойдёт, если выполнить `DELETE FROM tasks;` без WHERE?
6. Зачем при `docker run` указывать `-v todo-pg-data:/var/lib/postgresql/data`?

## Подготовка к модулю 2

В модуле 2 ты:

- Узнаешь про **миграции** — что это, зачем нужны, как их делать
- Сделаешь **простую систему** миграций через `.sql` файлы и `psql -f`
- Добавишь к схеме `created_at` (timestamp) — первое прикосновение к датам
- Узнаешь про **индексы** и зачем они нужны (хотя в нашем проекте они не критичны)
- Поработаешь с `ALTER TABLE` для эволюции схемы

К концу модуля 2 у тебя будет **репродуцируемая** структура БД: твою схему сможет создать с нуля любой человек одной командой `psql -f migrations/*.sql`.
